<p id="notice"><%= notice %></p>

<h1>Blogs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Date</th>
      <th>Body</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @blogs %> <!-- Inbuilt for the rails to render using the @blogs instance variable, initiated in the blogs controller -->
  </tbody>
</table>

<br>

<%= link_to 'New Blog', new_blog_path if logged_in?(:site_admin) %>


<!-- blogs ma present cha _blog.html.erb
     @blogs name cha instance variable ko
     so, @blogs ko instance variable lai we can use _blog.html.erb ko file, we are not trapped -->

